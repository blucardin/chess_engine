[workspace]
resolver = "3"
members = ["chess_engine", "chess_gui", "chess_neural_network_training"]

# Enable a small amount of optimization in the dev profile.
[profile.dev]
opt-level = 1

# Enable a large amount of optimization in the dev profile for dependencies.
[profile.dev.package."*"]
opt-level = 3